name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build (Release)
        run: dotnet build SellEverything/SellEverything.csproj -c Release --nologo

      - name: Collect plugin zip
        shell: bash
        run: |
          zip=$(find . -name 'latest.zip' -path '*Release*' | head -n1)
          if [ -z "$zip" ]; then
            echo "DalamudPackager latest.zip was not produced."
            echo "Zip files found:"; find . -name '*.zip' || true
            exit 1
          fi
          echo "Using: $zip"
          cp "$zip" SellEverything.zip

      - name: Resolve version
        id: ver
        shell: bash
        run: |
          v=$(grep -oE '<Version>[^<]+' SellEverything/SellEverything.csproj | sed 's/<Version>//')
          echo "version=$v" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $v"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SellEverything
          path: SellEverything.zip

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || steps.ver.outputs.version }}
          target_commitish: ${{ github.sha }}
          name: Sell Everything ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || steps.ver.outputs.version }}
          files: SellEverything.zip
          fail_on_unmatched_files: true
          generate_release_notes: true
